# AI開発ワークフロー監査（naotakagi）

- 調査日: 2026-07-07
- 対象期間: 主に 2026-05-18〜2026-07-07（約7週間）
- 証拠ソース: `tasks/`（307ファイルの git 履歴含む）、Flow リポジトリ（worktree 31個・open/merged PR・changelog）、`research/`・`products/*/STATUS.md`・`knowledge/model-routing.md`、Slack `proj-flow`（214件）、週次作業計画 7週分（`~/.nb/notes/`）、`~/.claude/projects/` セッション分布

---

## High Confidence Findings

### F1. 実装〜develop マージは速い。ここはボトルネックではない
naotakagi の直近マージ済み PR 34件の作成→マージは 0〜28時間、中央値は数時間。changelog は 06/22〜07/07 で1日10〜37件と高稼働。iOS/Desktop の PR も他領域と同等以上に速い（#3884 iOS Push backend 3h、#3873 pod install 1h）。Android のみ 16〜28h とやや遅いが許容範囲。**「未経験だから実装が遅い」という仮説はデータで否定される。**

### F2. 最大の詰まりは develop→main（release train）と QA evidence
- PR #3863（Release train）が 26時間超滞留: staging QA 総合判定 FAIL（P1 2件）、ECS deploy 3連続失敗、Desktop version bump gate ブロック、security review verdict `NEEDS-HUMAN-REVIEW` が重複。
- レビュー・QA 完了済みの #3853 が「現行 train が先」という理由で 37時間超の人為保留。**完成済みの仕事が train の詰まりに連鎖的にせき止められている。**
- `tasks/flow/Review/Next.js 16アップグレード.md` は PR マージ済みなのに「証跡未記録のため Review に残置」と本文に自認あり。**QA evidence が常にフローの後ろに剥がれ落ちる構造。**

### F3. 調査・監査ドキュメントの生成速度が、実装への吸収速度を大幅に超過している
- 2026-07-07 の単日で `research/` に約10本の新規文書（yjs-persistence、backend-benchmark、semantic-progress、completion-v1-deprecation、async-execution、graphql-performance、ios-release-audit、text-loss-audit 等）。
- `tasks/flow/` は Backlog **123** に対し Doing **7**。iOS 監査は1日で Backlog に10件超を注入。
- `research/2026-07-02-flow-additional-review-candidates.md` は「Fable 5換算20〜25本規模」の追加監査候補を列挙し、その P-7 は「未完了ブランチ約30本の棚卸し」— **棚卸しタスク自体が新たなバックログになっている。**

### F4. 「80%で止まる」の実体の大半は「他者判断・外部環境待ち」であり、それを検知する仕組みがない
- `tasks/air/in-progress/Q&A英語生成オプション.md`: チェックリスト 6/8 完了、担当者確認待ちのまま **93日間無更新**。
- セッション管理レビュー Phase 2: Phase 1 完了後「shuyuhey 判断待ち」のまま停止。
- Eigo #707: **8週連続**繰り越し（close 判断待ち）。Flow MSIX: **5週超**（Windows VM 環境待ち）。Eigo #661/#662/#668/#719: 「判断」のまま4週留置。
- いずれも Doing/in-progress レーンに置かれたままで、「待ち」と「作業中」が区別されていない。

### F5. 未経験プラットフォームの詰まりは実装ではなく「リリース要件の後追い発見」
- Sign in with Apple（Guideline 4.8）を実装終盤の 07-03 に把握（Slack proj-flow）。
- QA 表では「63件中58件実装済み」だったのに、07-07 のリリース前監査で sandbox 課金・別ノート画像混入・dev-token フォールバックの**リリースブロッカー3件**が発覚。
- Windows 配布も 05/12-13 に Authenticode 費用・SmartScreen・MSIX 制約を都度リサーチする後追い型だった。
- **「実装済み」と「リリース可能」の定義が分離しており、そのギャップは最後の監査まで見えない。** iOS 監査自体は非常に有効だった — 問題は実施タイミングが最後だったこと。

### F6. レビューは実装前に多重、実装後の検証は薄い
セッション管理レビューは実装前に「計画ブリーフ → Oracle 調査 → codex 5並列の Phase 1 → 判定レポート → Oracle による判定レポートの再検証」の**5層**。一方、iOS の実装済みコードのリリース可否検証は 07-07 まで存在しなかった。tasks の git 履歴では Review→Doing の差し戻しは **0件** — レビューが差し戻しを生んでいない（＝実装前レビューの多重化が防いでいる不良が実際にはあまり流れてきていない）ことの傍証。

### F7. 完了後のハウスキーピングが常態的に漏れている
- worktree 31個中: マージ済みブランチ＋未コミット21ファイルが11日放置（native-iap-billing）、msix-staging-ci は約1ヶ月放置。
- ゴーストタスク: `(Deprecated) In Progress` に委譲済みタスクが60日、期限超過の退去クローズタスクが60日。
- `tasks/flow/Backlog/ツール結果をartifact化...md` は **git 未追跡のまま約2ヶ月**。
- Flow #2532（chat-first）は close 判断の記録なく週次計画から無言で消滅 — 「残作業を黙って落とさない」原則（AGENTS.md）への抵触例。

### F8. 計画外作業が7週連続で20〜31%、うち一定部分はエージェント基盤自体のメンテ
週次計画の 🔀 比率: 26% / 26% / 29% / 31% / 27% / 20% / 22%。Codex/worktree の環境トラブル（起動失敗・ポート競合等）が約2週間おきに再発し、毎回計画外で吸収されている。

---

## Medium Confidence Hypotheses

### H1. AI が前半80%を安価にした結果、「80%完成在庫」が積み上がる均衡になっている
計画・実装・PR 作成のコストが Codex/Fable で激減した一方、ラスト20%（QA evidence、リリース判断、他者への催促、close 判断）は人間の時間と気力を要するまま。安い工程の生産量が高い工程の処理能力を超え、仕掛かりが在庫化する。Backlog 123 / Doing 7 という比率と 07-07 の文書10本はこの均衡の症状。

### H2. レビューの多重化は品質要求ではなく「不安のマネジメント」として消費されている
ユーザー自身が「初めての領域で前提知識が足りない」不安を自覚している。Oracle の使用が方針（深い調査に限定、正本にしない）を超えて連日化し、Phase 1 判定レポートを Oracle が再判定して深刻度を格上げする「事実上の最終判定者」になっている（model-routing.md のポリシーと乖離）。不安が強い領域ほどレビュー層が増え、しかし本当に不安を解消するもの（実機での end-to-end 検証）は増えていない。

### H3. メタ作業（モデルルーティング再設計・スキル整備）が、出荷が詰まったときの退避先になっている
07-04〜07-06 に model-strengths / agent-taxonomy / x-research-methods / claude-codex-interop 等へ集中投資。ちょうど release train が詰まり iOS がブロッカーを抱えていた時期と重なる。メタ作業は「進んでいる感」があるが出荷には直結しない。

### H4. 「実装=Codex、Claude Code=HQ/ナレッジ」という暗黙の分業により、QA・検証の担い手が空白になっている
セッション分布: HQ 477件・個人repo 192件に対し flow リポジトリの Claude Code セッションは15件のみ。Codex は実装して PR を出すが staging での動作検証はしない。Claude Code はナレッジを書くがアプリを触らない。結果、stg 最終確認はほぼ nao 一人が手動で担っている（Slack の「Stg確認して本番入れます」系発言はほぼ全て nao）。

### H5. 自分の PR スループットの高さが、チームのレビュー SLA 不在を隠している
他作者の PR は #3432（27日レビューなし）、#3650（13日 CONFLICTING のまま）、#2469/#2591（約4ヶ月）と放置。自分の PR が速いためチャンネル全体では回っているように見えるが、チームとしてのレビュー保証は存在しない。

---

## Repeated Failure Patterns

| # | パターン | 実例 |
|---|---|---|
| P1 | **判断待ちリンボー**: 他者判断・確認待ちを Doing に置いたまま、催促もエスカレーションもされず数週〜数ヶ月 | Q&A英語生成93日、セッション管理Phase2、Eigo #707 8週、#661/#662/#668/#719 4週 |
| P2 | **エビデンス後回し**: 実装・マージは済むが QA 証跡が残らず、タスクが Review に残置 or 「実装済み」表記が実態と乖離 | Next.js 16、iOS QA表 58/63 vs ブロッカー3件 |
| P3 | **監査が監査を生む**: 監査・調査の成果物が実装ではなく新たな監査候補・Backlog を生成 | additional-review-candidates（20〜25本）、text-loss audit の「宙に浮いたリスク」5項目、iOS監査→Backlog 10件超 |
| P4 | **静かなフェードアウト**: close 判断の記録なくタスクが視界から消える | Flow #2532、(Deprecated) In Progress のゴースト2件 |
| P5 | **マージ後の置き土産**: PR マージ後に worktree・重複作業・未コミットファイルが残る | native-iap worktree（21ファイル11日）、msix-staging-ci（1ヶ月）、ios-push-backend の重複worktree |
| P6 | **リリース要件の後追い発見**: 未経験プラットフォームで、審査・署名・課金などの「出荷ゲート要件」を実装後に知る | Sign in with Apple（07-03）、sandbox課金、Authenticode/MSIX（05/12-13） |
| P7 | **環境・移行系タスクの見積もり崩壊**: deps upgrade → CI fail → 調査のループと外部環境待ちで、1週間見積もりが5〜8週化 | Eigo deps クラスタ（#711→#714→#739→#748/#749）、MSIX、Rails 7.2 |

---

## Workflow Bottlenecks

| 工程 | 状態 | 診断 |
|---|---|---|
| PRD / 計画 | **過剰気味** | plan-brief + oracle-response ペアがほぼ連日生成。セッション管理は実装前に5層。計画の質は高いが、計画→実装の変換率が管理されていない |
| TODO / タスク管理 | **肥大** | Backlog 123。監査由来の一括注入があり、triage されないまま積み上がる。Todo にも再編由来の59日滞留12件 |
| Codex 依頼・実装 | **良好** | 単機能 PR の粒度が確立しており当日〜翌日でマージ。ここは現状維持でよい |
| レビュー（実装前） | **過多** | 5層構造、Oracle の連日使用、差し戻し実績0件。防いでいる不良に対してコストが見合っていない可能性 |
| レビュー（他者PR） | **不在** | 他作者 PR が数週〜4ヶ月放置 |
| CI | 概ね良好 | 個別 PR の CI は速い。ただし release train の ECS deploy が3連続失敗するなどインフラ起因の不安定さが毎リリースの税金 |
| QA | **最薄・属人** | stg 確認は nao ほぼ一人。QA evidence の記録が構造化されておらず後回し。staging-qa スキル（06/17〜）で半形式化したが浸透途上 |
| PR → develop | 良好 | 中央値数時間 |
| リリース（develop→main） | **最大の詰まり** | train が P1 バグ + deploy 障害で26時間超停止し、完成済み PR を連鎖的にせき止める |
| クローズ / 片付け | **欠落** | worktree cleanup・close 判断・evidence 記録が誰の仕事でもない |

---

## Tool Allocation

現行の `knowledge/model-routing.md` は方針として概ね正しい。問題はポリシーではなく運用のドリフト。

- **Codex（実装）**: 現状の「単機能 PR 粒度」が最適で、実績も良い。変えるべきは粒度ではなく**依頼に含める完了定義**。「実装完了」ではなく「stg で動作確認できる状態＋確認手順の記載」までを依頼のスコープに含める。
- **Claude Code（通常モデル / scout・builder）**: 証跡収集・突合・棚卸し・ハウスキーピングの自動化に回す。今回の監査で使ったような「収集は sonnet、統合は上位」の構造をルーティン化する。
- **Fable**: (1) 未経験領域の**着手前**リリース要件監査、(2) 出荷直前の最終レビュー1本、(3) 2時間以上ハマった問題の解決、に限定。実装前の多層レビューの1層として消費しない。
- **Oracle review**: 自分のポリシー（判断材料の生成器、正本にしない）に戻す。**実装前の判定役から外し**、週の上限回数を決める（例: 週2回）。出す前の自己点検: 「この質問の答えで自分の次のアクションが変わるか？ 変わらないなら不安の解消目的であり、出さずに実機検証で解消する」。
- **人間（自分）**: リリース判断、P0/P1 裁定、判断待ちのエスカレーション（催促）、close 判断。**「待ち」を管理するのは AI に委譲できない自分の専任業務**と再定義する。
- **人間（チーム）**: 判断待ち項目は依頼時に期限を付けて渡す。「いつでもいいので見てください」は93日コースになることが実証済み。

---

## Blind Spots（未知の未知）

1. **QA 表・STATUS が「古い鏡」になっている**: STATUS.md の誤記を 07-07 に自分で訂正した（commit 96d04723）ように、進捗ドキュメントが実態とずれ、そのずれた鏡を見て安心してしまう。ドキュメントは「実態が先、文書は後」の原則通り、リリース判断の根拠には**実機検証のみ**を使う。
2. **リリースの単一障害点が自分**: stg QA の判断者が実質1人。自分が iOS に集中した週は他の全 train が遅れる構造。staging-qa スキルの完全な手順化と、P1 判定基準の明文化で他メンバー/エージェントに委譲可能にする。
3. **ECS deploy の不安定さが「毎回起きる例外」として正常化している**: 3連続失敗をリトライで流しており、根本対応がタスク化されていない。リリース頻度が上がるほど税率が上がる。
4. **research 文書の「reflected」規約が形骸化しつつある**: `reflected: false` のまま放置された retro が複数。良い知見が書かれても回収されない。
5. **git 未追跡のタスクファイル**: ボードに見えているのに履歴がないタスクが存在（2ヶ月）。棚卸しスクリプトに `git ls-files` 突合を入れないと検知できない。
6. **「時間の使い方」のログがない**: 週次計画に🔀は付くが、レビュー・メタ作業・QA に何時間使ったかは記録されておらず、本監査でも比率は推定しかできない。レビュー過多の定量判断には最低限の時間タグが要る。

---

## Process Changes（すぐ変える運用）

1. **Blocked レーンを新設**: `tasks/{board}/Blocked/`。判断待ち・外部環境待ちは Doing から移し、frontmatter に「待ち先」「次に催促する日」を必須化。7日ごとに催促、14日で Backlog 降格か close を判断。
2. **research → task 変換ゲート**: research 文書は作成時に末尾へ `disposition: implement-now | task化(リンク) | archive` を必ず書く。72時間 disposition なしの文書は archive 扱い。監査系文書は「発見したリスク数」ではなく「タスク化して着手した数」を成果とする。
3. **audit-first**: 未経験プラットフォーム・外部審査があるものは、**着手前に**リリース要件監査（07-07 iOS 監査のテンプレを転用: 審査ガイドライン・署名/配布・課金環境・認証フェイルセーフ）を実施し、その結果を PRD の完了条件に焼き込む。
4. **QA evidence をマージ条件に前倒し**: 「stg 確認して本番入れます」を口頭運用からやめ、PR テンプレに evidence 欄（確認手順・結果・スクショ/ログ）を設け、記入済みを train 投入条件にする。
5. **マージ後クリーンアップの自動化**: PR マージをトリガに `/wt-cleanup` 対象を検出する週次スイープ（scout に委譲可能）。「棚卸し」を監査プロジェクトにせず、30分の定例作業に閉じ込める。
6. **Oracle の役割を戻す**: 実装前フローから外し週2回上限。判定の正本は自分（＋必要なら Fable 1本）。
7. **ECS deploy 不安定を P0 インフラタスクとして起票**: リトライ運用の正常化をやめる。
8. **Doing WIP 上限 5**（Blocked 分離後）。6件目に着手したくなったら、既存のどれかを Done/Blocked/Backlog へ動かしてから。

---

## Standard Workflow Proposal

### 作業開始前チェック（新テーマ着手時）
- [ ] Doing は 5 件未満か（超えるなら着手しない）
- [ ] 出荷ゲート要件は洗い出したか（外部審査・署名・課金・認証があるなら audit-first を先に）
- [ ] 完了条件に「stg 検証手順」と「evidence の置き場所」が書いてあるか
- [ ] このテーマの判断者は誰か（自分以外なら、判断依頼の期限を先に合意）

### Codex への渡し方
- 単機能 PR 粒度は現状維持
- 依頼に必ず含める: 完了条件 / 変更範囲外（touch しないもの）/ 検証手順（Codex 自身が実行できる範囲のテスト・確認コマンド）
- 「実装＋その動作確認手順の Markdown」までがワンセット。手順が書けない実装は受け取らない

### Fable にだけ頼むレビュー・作業
- 未経験領域の着手前リリース要件監査
- train 投入直前の最終レビュー（1回・1本）
- 2時間ルール超えのハマり解決（調査→修正まで一気通貫）

### Oracle review に出す前の自己点検
- この回答で次のアクションが変わるか？（変わらない → 出さない）
- 求めているのは「情報」か「安心」か？（安心 → 実機検証で解消する）
- 今週すでに2回使ったか？（使った → 来週へ）

### PR 作成前チェック
- [ ] 自分で1回 end-to-end で動かしたか（/verify 相当）
- [ ] evidence 欄が埋まっているか
- [ ] この PR で不要になる worktree・ブランチ・タスクを特定したか

### CI 失敗時の切り分け
1. 自分の変更起因か、train/インフラ起因か（他 PR でも落ちているかをまず見る）
2. インフラ起因なら自分で深追いせず、インフラタスクに記録して切り離す（ECS 3連続失敗をその場のリトライで吸収しない）
3. 自分起因なら再現最小化 → 2時間で解けなければ Fable へ

### 途中で迷走した時の停止ルール
- 同じエラー・同じ論点に2時間 → 手を止め、「分かっていること / 試したこと / 仮説」を5行で書き出してから委譲判断
- 「もう1層レビューを足したい」と感じたら → 足す前に、その不安を実機でどう検証できるかを1つ書く。書けたらレビューではなく検証をやる

### 週次棚卸し
下記テンプレを毎週月曜の週次計画と同時に実施（30分タイムボックス。棚卸し自体をプロジェクト化しない）。

---

## Stop Rules

1. **WIP**: Doing 5 件超で新規着手禁止。
2. **判断待ち**: Blocked 移動時に催促日を設定。7日で催促、14日で降格/close 判断。「無言のフェードアウト」（#2532型）を禁止し、close は1行でも記録する。
3. **レビュー層**: 1変更につき実装前1層＋実装後1層まで。3層目を足す前に「この層が検出する想定の不良は何か」を書けなければ足さない。
4. **research 生成**: 同一テーマで disposition 未処理の research 文書が2本ある状態で3本目を書かない。
5. **ハマり**: 2時間で停止 → 切り分けメモ → 委譲。
6. **メタ作業**: スキル・ルーティング・環境整備は週2時間の箱に収める。箱を超える分は翌週の計画に「計画として」入れる（🔀で吸収しない）。
7. **監査**: 新しい監査を始める条件は「前回の監査の P0/P1 がすべてタスク化され、P0 は着手済み」であること。

---

## Weekly Review Template

```markdown
# 週次棚卸し YYYY-MM-DD（30分タイムボックス）

## 1. 出荷したもの（main に届いたもの。develop 止まりは含めない）
-

## 2. Doing 監査（5件以内か）
| タスク | 経過日数 | 今週動いたか | 処置（続行/Blocked/Backlog/close） |

## 3. Blocked 監査
| タスク | 待ち先 | 待ち日数 | 今週の催促（する/した/期限切れ→降格） |

## 4. Review レーン監査（evidence 残置がないか）
| タスク | 残っている理由 | evidence 収集を今週やるか |

## 5. research 変換率
- 今週生成: N本 / disposition 済み: N本 / 実装着手に至った: N本
- disposition 未処理が2本以上あるテーマ:

## 6. ハウスキーピング（15分）
- [ ] マージ済みブランチの worktree を /wt-cleanup
- [ ] git 未追跡のタスクファイルがないか（git status で tasks/ を確認）
- [ ] 週次計画から無言で消えた項目がないか（先週の⏳と今週の計画を突合）

## 7. 計画外比率と原因
- 🔀比率: __%（20%超なら最多原因を1つ特定。環境トラブルなら再発防止をタスク化）

## 8. 来週「出荷まで持っていく」1テーマ
- （複数書かない。これだけは train 投入・リリースまで到達させる）
```

---

## Next 7 Days Action Plan

| 日 | アクション |
|---|---|
| Day 1 | **release train #3863 を出荷しきる**ことだけに集中: P1 2件の裁定、ECS 失敗の切り分け（インフラタスク起票）、train 通過。#3853 も乗せる |
| Day 2 | Blocked レーン新設＋既存の判断待ち4件（セッション管理Phase2、Q&A英語生成、Eigo #707、#661/#662/#668/#719）を移動し、それぞれ催促を1本ずつ送る（期限付き） |
| Day 3 | worktree スイープ: マージ済み・重複の worktree（native-iap、msix-staging-ci、ios-push-backend 等）を /wt-cleanup。未コミット差分は要否を10分で判断して捨てるか退避 |
| Day 4 | research 棚卸し: 07-07 生成の約10本に disposition を付ける。「今やる」は1〜2本に絞り、残りはタスク化 or archive |
| Day 5 | iOS ブロッカー3件のうち P0（sandbox 課金・dev-token フォールバック）に着手。監査テンプレを `knowledge/` に「着手前リリース要件監査」として一般化 |
| Day 6 | PR テンプレに QA evidence 欄を追加、staging-qa スキルとの接続を確認。Next.js 16 の残 evidence 2件を回収して Review を空にする |
| Day 7 | 週次棚卸しテンプレの初回実施。Oracle 週2回上限・レビュー2層ルールを model-routing.md に追記して運用開始 |

---

## 付記: うまくいっているので変えないもの

- Codex への単機能 PR 粒度の委譲（中央値数時間でマージ）
- changelog 運用と develop の高頻度マージ
- Review→Doing 差し戻し0件（タスク粒度と事前整理の質は高い）
- 07-07 型のリリース前監査そのもの（実施タイミングを「最後」から「最初」に動かすだけでよい）
